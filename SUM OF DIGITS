def sum_of_digits(number):
    digit_sum = 0
    for digit in str(number):
        digit_sum += int(digit)
    return digit_sum

# Example usage
number = 123
result = sum_of_digits(number)

print("Sum of Digits =", "+".join(str(digit) for digit in str(number)), "=", result)
